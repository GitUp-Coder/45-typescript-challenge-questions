/*Question no 11 : Names: Store the names of a few of your friends in a array called names.
Print each person’s name by accessing each element in the list, one at a time.*/
// Declare an array named 'name' that stores strings
let name = ["Arslan", "Maryam", "Tahreem", "Mahnoor", "Tawer", "Arbab"];
// Use the forEach method to iterate over each element in the 'name' array
name.forEach(name => {
    // For each element, print the name to the console
    console.log(name);
});
export {};
